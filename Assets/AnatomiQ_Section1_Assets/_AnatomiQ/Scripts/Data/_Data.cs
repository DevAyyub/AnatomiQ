// AnatomiQ.Data — Schema layer (lowest layer; no app-internal dependencies).
// Will hold: OrganAsset (incl. v1.1 nodeType + fmaId) and DiseaseAsset ScriptableObject
// schemas with their nested types and enums, plus the IDataLayer contract.
// No types yet — added in Section 7. Empty namespace compiles to an empty assembly.
namespace AnatomiQ.Data
{
}
